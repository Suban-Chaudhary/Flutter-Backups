import 'package:flutter/material.dart';

class DisplayWeatherInformation extends StatefulWidget {
  const DisplayWeatherInformation({Key? key}) : super(key: key);

  @override
  _DisplayWeatherInformationState createState() =>
      _DisplayWeatherInformationState();
}

class _DisplayWeatherInformationState extends State<DisplayWeatherInformation> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
